/// Represents a single completed calculation with its expression and result.
class Calculation {
  final String expression;
  final String result;

  const Calculation(this.expression, this.result);
}
